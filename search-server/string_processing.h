#pragma once
#include <string>
#include <vector>
#include <set>

std::vector<std::string_view> SplitIntoWordsView(const std::string_view& text);
std::vector<std::string_view> SplitIntoWordsViewS(const std::string_view& text);
